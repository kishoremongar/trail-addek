import React from "react";
import Hero from "../components/Hero/Hero";

const Home = () => {
  return (
    <main className="homeContainer bg-image">
      <Hero />
    </main>
  );
};

export default Home;
